import os
def cc(n):
    print(n)
#field = [f for f in reacted._meta.fields]
#field = [exec(f"{cc(f)}") for f in reacted._meta.fields]
#print(field,len(field))
react = "getcwd()"
exec(f"name=os.{react}\ncc(name)\nprint('EXEC',name)")
exec("print('helo')\n"
    "print('world')"
)